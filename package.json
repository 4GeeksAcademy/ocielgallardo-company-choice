{
  "name": "ocielgallardo-company-choice",
  "private": true,
  "scripts": {
    "start:playground": "npx --yes http-server . -p 3000 -a 0.0.0.0",
    "docker:up": "docker compose up --build",
    "docker:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build"
  }
}
