# _template-service

Template placeholder for future backend services.

## Suggested internal structure

- `src/domain/` business entities and rules
- `src/application/` use cases and orchestration
- `src/infrastructure/` adapters (db, messaging, external APIs)
- `src/interfaces/http/` transport layer
- `tests/` unit and integration tests

TODO: Confirm standard runtime (Node/Fastify/Nest or alternative) before generating templates.
